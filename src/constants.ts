// file extension
const file_extension = "bfg";

// language
const language = "en";

// sample size for checking null character
const sample_size = 512;

export {
    file_extension,
    language,
    sample_size
}
